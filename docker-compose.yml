services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: interop
      POSTGRES_USER: interop
      POSTGRES_PASSWORD: interop
    ports:
      - "5432:5432"
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

  app:
    build: ./data-event-ai-caseware
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/interop
      RANKER_URL: http://ranker:8000
    volumes:
      - lake:/app/lake
      - state:/app/state
      - events:/app/events

  ranker:
    build: ./data-event-ai-ranker
    ports:
      - "8000:8000"
    environment:
      LAKE_CASES_PATH: /app/lake/cases
    volumes:
      - lake:/app/lake
    depends_on:
      - app

volumes:
  lake:
  state:
  events: